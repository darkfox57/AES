import styled from "styled-components";

export const Detail_Body = styled.section`
display: flex;
flex-direction: column;
gap: 10px;
a {
 text-decoration: none;
 color: var(--dash-color);
 font-size: 2rem;
 span {
  font-size: 1.5rem;
 }
}
`